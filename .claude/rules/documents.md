# 문서 (Skill)

`.claude/skills/` 아래 프로젝트 산출물 관리:
- `requirements/` 요건정의, `functional-spec/` 기능명세, `screen-spec/` 화면명세
- `db-design/` DB설계, `api-spec/` API명세, `coding-standards/` 코딩표준
- `test-plan/`, `test-data/`, `test-result/` 테스트 산출물
